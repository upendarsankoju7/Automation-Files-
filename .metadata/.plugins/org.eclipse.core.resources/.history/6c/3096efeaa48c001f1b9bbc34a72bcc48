package Automation_Opencart;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver; // Importing WebDriver interface for browser automation
import org.openqa.selenium.chrome.ChromeDriver; // Importing ChromeDriver class to use the Chrome browser

public class Css_Selectors_1 {

    public static void main(String[] args) {
        // Main method - Entry point of the Java program

        // Creating an instance of ChromeDriver to launch the Chrome browser
        WebDriver driver = new ChromeDriver();
        
        // Navigating to the demo nopCommerce website
        driver.get("https://demo.nopcommerce.com	/");
        
        // Maximizing the browser window to full screen
        driver.manage().window().maximize();
        
        //Tag and id CSS Selector 	tag#id 
        
        
        //driver.findElement(By.cssSelector("input#small-searchterms")).sendKeys("Iphone");

        // driver.findElement(By.cssSelector("#small-searchterms")).sendKeys("Iphone");
        
        //Tag And className CSS Selector 
        
        
          //driver.findElement(By.cssSelector("input.small-searchterms")).sendKeys("Iphone");
          //driver.findElement(By.cssSelector (".small-searchterms")).sendKeys("Iphone");
        
        
        
        
    }
}
